package version

// Version is the application version shown by `fedishare --version`.
// Keep this as a development version until a release is tagged.
const Version = "0.6.0-dev"

// AppName is the user-visible product name.
const AppName = "FediShare"
