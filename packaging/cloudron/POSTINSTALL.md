Set **gateway_url** on the FediShare desktop app to this location's HTTPS origin (for example `https://nodes.example.org`), with no trailing slash.

The actor private key never leaves the desktop. This gateway only stores the public key and a small Actor/WebFinger cache.
